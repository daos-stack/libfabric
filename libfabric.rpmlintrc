# This line is mandatory to access the configuration functions
from Config import *

# on Leap 15, since the library is in a libfabric1 package, the docs end up
# being larger than the tools in the main libfabric package
addFilter('package-with-huge-docs')

# https://github.com/rpm-software-management/rpmlint/issues/856
addFilter('incoherent-version-in-changelog')
